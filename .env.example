WEB_PORT=8088
WEB_ADMIN_USER=admin
# 로그인 비밀번호 설정 방법 (택1):
#  1) 아래 WEB_ADMIN_PASSWORD 에 평문 입력 → 부팅 시 bcrypt 해시로 변환·영속화.
#     (주의: 평문 env는 docker inspect 로 보임. 첫 부팅 후 이 줄을 지워도 로그인 유지됨)
#  2) 비워두면 첫 부팅 시 랜덤 비밀번호를 생성해 컨테이너 로그에 1회 출력.
#  3) 더 안전: secrets/web-admin.hash 에 bcrypt 해시를 직접 넣으면 그게 최우선.
WEB_ADMIN_PASSWORD=
REMOTE_NAME=gdrive
HOST_TAG=instance-20260108-1630
ALLOW_REPO_INIT=false
# rclone 설정 웹 GUI 포트(5572) 바인드 주소.
#  127.0.0.1 (기본) = 서버 로컬만, SSH 터널로 접속(가장 안전)
#  0.0.0.0 = 호스트 모든 인터페이스(상위에서 NAT/도메인 포워딩으로 접근 통제될 때만)
RCLONE_GUI_BIND=127.0.0.1
